# Internal API base URL for server-side fetches (SSR / ISR / sitemap)
API_BASE_INTERNAL=http://localhost:8000

# Public API base URL exposed to browser clients
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Canonical base URL for SEO metadata, sitemap, and robots.txt
NEXT_PUBLIC_APP_URL=http://localhost:3000
